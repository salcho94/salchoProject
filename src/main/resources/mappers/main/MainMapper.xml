<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.salcho.project.front.main.dao.MainDAO">
    <select id="get" parameterType="string" resultType="mainDto">
     SELECT * FROM front_header WHERE path = #{path};
    </select>
</mapper>
